
<%= simple_form_for(@cocktail) do |f| %>
<%= f.input :name, :label => false, wrapper_html: {class: "form-inline"} do %>
  <%= f.input_field :name, :label => false, placeholder: "Name" %>
  <%= f.button :submit, "Apply", :class => "btn-warning" %>
<% end %>

<% end %>
